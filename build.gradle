ext {
    springBootVersion = '2.1.3.RELEASE'
}

allprojects {
    group = 'com.example'
    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'java'
    version = '1.0'

    buildscript {
        repositories {
            mavenCentral()
        }
    }
}